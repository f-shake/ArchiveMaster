<v:VerticalTwoStepPanelBase
    x:Class="ArchiveMaster.Views.TimeClassifyPanel"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ct="using:FzLib.Avalonia.Controls"
    xmlns:cvt="using:ArchiveMaster.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:v="using:ArchiveMaster.Views"
    xmlns:vm="clr-namespace:ArchiveMaster.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:DataType="vm:TimeClassifyViewModel"
    mc:Ignorable="d">
    <v:VerticalTwoStepPanelBase.ConfigsContent>
        <ct:StackFormItemGroup>
            <ct:FormItem Label="目录：">
                <v:FilePickerTextBox
                    FileNames="{Binding Config.Dir}"
                    Type="OpenFolder" />
            </ct:FormItem>
            <ct:FormItem Label="最小间隔（分）：">
                <NumericUpDown
                    FormatString="0"
                    Maximum="1440"
                    Minimum="1"
                    Value="{Binding Config.MinTimeInterval, Converter={x:Static cvt:Converters.TimeSpanNumber}, ConverterParameter=m}" />
            </ct:FormItem>
        </ct:StackFormItemGroup>
    </v:VerticalTwoStepPanelBase.ConfigsContent>
    <v:VerticalTwoStepPanelBase.ResultsContent>
        <Grid ColumnDefinitions="*,8,*">
            <HeaderedContentControl
                Classes="GroupBox"
                Header="按修改时间分类的目录">
                <ListBox
                    x:Name="lstDirs"
                    DisplayMemberBinding="{Binding Name}"
                    ItemsSource="{Binding SameTimePhotosDirs}" />
            </HeaderedContentControl>
            <HeaderedContentControl
                Grid.Column="2"
                Classes="GroupBox"
                Header="目录内文件">
                <v:SimpleFileDataGrid
                    ColumnIsCheckedIndex="-1"
                    ColumnStatusIndex="-1"
                    ItemsSource="{Binding #lstDirs.SelectedItem.Subs}" />
            </HeaderedContentControl>
        </Grid>
    </v:VerticalTwoStepPanelBase.ResultsContent>
</v:VerticalTwoStepPanelBase>
