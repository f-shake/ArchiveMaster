<v:VerticalTwoStepPanelBase
    x:Class="ArchiveMaster.Views.VideoInfoPanel"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ct="using:FzLib.Avalonia.Controls"
    xmlns:cvt="using:ArchiveMaster.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:v="using:ArchiveMaster.Views"
    xmlns:vm="clr-namespace:ArchiveMaster.ViewModels"
    x:Name="root"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:DataType="vm:TimeAssViewModel"
    mc:Ignorable="d">
    <v:VerticalTwoStepPanelBase.ConfigsContent>
        <ct:FormItem Label="目录：">
            <v:FilePickerTextBox
                FileNames="{Binding Config.Dir}"
                Filter="{Binding Config.Filter}"
                IsFilterBarVisible="True"
                Type="OpenFolder" />
        </ct:FormItem>
    </v:VerticalTwoStepPanelBase.ConfigsContent>
    <v:VerticalTwoStepPanelBase.ResultsContent>
        <UniformGrid
            Columns="1"
            RowSpacing="8"
            Rows="2">
            <v:SimpleFileDataGrid
                ItemsSource="{Binding Files}"
                SelectedItem="{Binding SelectedFile}">
                <v:SimpleFileDataGrid.Columns>
                    <DataGridTextColumn
                        Binding="{Binding VideoInfo.Format.FormatLongName}"
                        Header="格式"
                        IsReadOnly="True" />
                    <DataGridTextColumn
                        Binding="{Binding VideoInfo.Format.StreamCount}"
                        Header="流数量"
                        IsReadOnly="True" />
                    <DataGridTextColumn
                        Binding="{Binding VideoInfo.Format.Duration, Converter={x:Static cvt:Converters.SecondsToTimeSpanString}}"
                        Header="时长"
                        IsReadOnly="True" />
                    <DataGridTextColumn
                        Binding="{Binding VideoInfo.Format.BitRate, Converter={x:Static cvt:Converters.BitRateMbps}, Mode=OneWay}"
                        Header="码率"
                        IsReadOnly="True" />
                </v:SimpleFileDataGrid.Columns>
            </v:SimpleFileDataGrid>
            <DataGrid
                IsReadOnly="True"
                ItemsSource="{Binding SelectedFile.VideoInfo.Streams}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Binding="{Binding Index}"
                        Header="序号" />
                    <DataGridTextColumn
                        Binding="{Binding CodecType, Converter={x:Static cvt:Converters.Description}, Mode=OneWay}"
                        Header="类型" />
                    <DataGridTextColumn
                        Binding="{Binding CodecLongName}"
                        Header="编码" />
                    <!-- <DataGridTextColumn Header="类型"> -->
                    <!--     <DataGridTextColumn.Binding> -->
                    <!--         <MultiBinding StringFormat="{}{0} ({1})"> -->
                    <!--             <Binding Path="CodecShortName" /> -->
                    <!--             <Binding Path="CodecLongName" /> -->
                    <!--         </MultiBinding> -->
                    <!--     </DataGridTextColumn.Binding> -->
                    <!-- </DataGridTextColumn> -->

                    <DataGridTextColumn
                        Binding="{Binding BitRate, Converter={x:Static cvt:Converters.BitRateMbps}, Mode=OneWay}"
                        Header="码率" />
                    <DataGridTextColumn
                        Binding="{Binding Duration, Converter={x:Static cvt:Converters.SecondsToTimeSpanString}, Mode=OneWay}"
                        Header="时长" />
                    <DataGridTextColumn
                        Binding="{Binding Profile}"
                        Header="预设" />
                    <DataGridTextColumn
                        Binding="{Binding Size}"
                        Header="分辨率" />
                    <DataGridTextColumn
                        Binding="{Binding ColorRange}"
                        Header="色彩范围" />
                    <DataGridTextColumn
                        Binding="{Binding ColorSpace}"
                        Header="色彩空间" />
                    <DataGridTextColumn
                        Binding="{Binding TargetFrameRate, StringFormat=F2}"
                        Header="目标帧率" />
                    <DataGridTextColumn
                        Binding="{Binding ActualFrameRate, StringFormat=F2}"
                        Header="实际帧率" />
                    <DataGridTextColumn
                        Binding="{Binding FrameCount}"
                        Header="帧数量" />

                </DataGrid.Columns>
            </DataGrid>
        </UniformGrid>
    </v:VerticalTwoStepPanelBase.ResultsContent>
</v:VerticalTwoStepPanelBase>